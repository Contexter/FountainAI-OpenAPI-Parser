components:
  schemas:
    ExampleSchema:
      properties:
        example_field:
          type: string
      type: object
info:
  title: Sample API
  version: 1.0.0
openapi: 3.0.0
paths:
  /example:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  example_field:
                    type: string
                type: object
